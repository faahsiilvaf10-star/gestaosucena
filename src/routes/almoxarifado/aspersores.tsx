import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/almoxarifado/aspersores')({
  component: () => <div className="p-8">Página de Aspersores em construção</div>,
})