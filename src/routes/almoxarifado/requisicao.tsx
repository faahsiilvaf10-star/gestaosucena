import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/almoxarifado/requisicao')({
  component: () => <div className="p-8">Página de Requisição em construção</div>,
})